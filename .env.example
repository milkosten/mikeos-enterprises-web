# Bearer token for the registry backends (same OSM_TOKEN the maps stack uses).
# Server-side only — never shipped to the browser.
OSM_TOKEN=changeme
# X-API-Key for the internal mikeos-sweden-scb service (named key "enterprises-web"
# in its INTERNAL_API_KEYS). Server-side only.
SCB_KEY=changeme
